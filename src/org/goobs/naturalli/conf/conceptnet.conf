include "base.conf"

learn {
  iterations  = 10000
  model {
    dir   = /home/gabor/workspace/naturalli/models/ConceptNet/
    start = 0
  }
  train = "{conceptnet_mturk_train}"
  test  = "{conceptnet}"
}

#search.allowlookup = false

evaluate.model = "models/ConceptNet/emnlp2014_final.tab"

